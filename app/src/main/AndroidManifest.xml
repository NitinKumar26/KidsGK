<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gadgetsfolk.kidsgk">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/admob_app_id" />

        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>

        <activity android:name=".activity.MainActivity" />

        <activity android:name=".activity.QuizCategoryActivity"
            android:parentActivityName=".activity.MainActivity"
            android:label="Quiz Type"/>

        <activity android:name=".activity.QuizSubCategoryActivity"
            android:label="Quiz"
            android:parentActivityName=".activity.QuizCategoryActivity"/>

        <activity android:name=".activity.QuizActivity" />

        <activity android:name=".activity.ScoreActivity"
            android:label="Your Score Card"/>

        <activity android:name=".activity.VideoActivity"
            android:parentActivityName=".activity.MainActivity"
            android:label="Videos"/>

        <activity android:name=".activity.PlayerActivity"
            android:screenOrientation="landscape"/>

    </application>

</manifest>